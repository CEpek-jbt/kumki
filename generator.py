import fontforge

from components import po, uppercircle

def kumki_generator(font, weight):

 glyph = font.createChar(ord("p"), "p")
    pen = glyph.glyphPen()
    po(pen, fw, wd)
    uppercircle(p, fw, wd)
